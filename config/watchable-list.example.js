'use strict';

// it doesn't accept folders, only domain addresses
// e.g. `www.google.com.br`
// not `www.google.com.br/meu-site/teste`

let sites = [
    "www.google.com.br",
    "www.github.com",
];

module.exports = sites;
